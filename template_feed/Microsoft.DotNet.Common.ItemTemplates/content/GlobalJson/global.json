{
  "sdk": {
    //#if (RollForward!="" && (!NoSdkVersion || AllowPrerelease!=""))
    "rollForward": "ROLL_FORWARD_VALUE",
    //#elseif (RollForward!="" && NoSdkVersion && AllowPrerelease=="")
    "rollForward": "ROLL_FORWARD_VALUE"
    //#endif
    //#if (AllowPrerelease!="" && !NoSdkVersion)
    "allowPrerelease": ALLOW_PRERELEASE_VALUE,
    //#elseif (AllowPrerelease!="" && NoSdkVersion)
    "allowPrerelease": ALLOW_PRERELEASE_VALUE
    //#endif
    //#if (!NoSdkVersion)
    "version": "SDK_VERSION"
    //#endif
  //#if (TestRunner=="")
  }
  //#else
  },
  "test": {
    "runner": "TEST_RUNNER_VALUE"
  }
  //#endif
}
