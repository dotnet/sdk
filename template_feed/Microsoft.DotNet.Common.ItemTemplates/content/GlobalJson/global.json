{
  "sdk": {
    //#if (RollForward!="")
    "rollForward": "ROLL_FORWARD_VALUE",
    //#endif
    //#if (AllowPrerelease!="")
    "allowPrerelease": ALLOW_PRERELEASE_VALUE,
    //#endif
    "version": "SDK_VERSION"
  //#if (TestRunner=="")
  }
  //#else
  },
  "test": {
    "runner": "TEST_RUNNER_VALUE"
  }
  //#endif
}
