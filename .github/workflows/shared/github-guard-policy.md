---
# Shared GitHub guard policy for the SDK repository.
# Provides the approval label that allows cookie-approved issues to bypass the
# min-integrity check when the GitHub MCP server performs tool calls.
tools:
  github:
    approval-labels: [cookie]
---
