name: Inter-branch merge workflow
on:
  push:
    branches:
      - main
      - release/**

permissions:
  contents: write
  pull-requests: write

env:
  DOTNET_CLI_TELEMETRY_SESSIONID: gha-${{ github.repository_id }}-${{ github.run_id }}-${{ github.run_attempt }}

jobs:
  Merge:
    uses: dotnet/arcade/.github/workflows/inter-branch-merge-base.yml@681c69ca40cc5ce1eccafb742e2cc275422e5181 # main