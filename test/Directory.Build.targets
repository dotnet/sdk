<Project>

  <PropertyGroup>
    <MSBuildAllProjects>$(MSBuildAllProjects);$(MSBuildThisFileFullPath)</MSBuildAllProjects>
  </PropertyGroup>

  <PropertyGroup Condition="'$(IsTestProject)' == 'true'">
    <!-- Set this to true for test project only because test assets contains .editorconfig file -->
    <NoDefaultExcludes>true</NoDefaultExcludes>
  </PropertyGroup>

  <!-- SDK test projects use FluentAssertions and Microsoft.NET.TestFramework.
       TemplateEngine test projects don't use these - they use xunit + FakeItEasy + Verify. -->
  <ItemGroup Condition="'$(IsTestProject)' == 'true' AND '$(_IsTemplateEngineTestProject)' != 'true'">
    <Using Include="FluentAssertions" />
    <Using Include="Microsoft.NET.TestFramework" />
    <Using Include="Microsoft.NET.TestFramework.Assertions" />
    <Using Include="Microsoft.NET.TestFramework.Commands" />
    <Using Include="Microsoft.NET.TestFramework.ProjectConstruction" />
    <Using Include="Microsoft.NET.TestFramework.Utilities" />
    <Using Include="Xunit" />
    <Using Include="Xunit.Abstractions" />
  </ItemGroup>

  <!-- TE test projects only need basic Xunit global using -->
  <ItemGroup Condition="'$(IsTestProject)' == 'true' AND '$(_IsTemplateEngineTestProject)' == 'true'">
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup Condition="'$(IsTestProject)' == 'true' AND '$(_IsTemplateEngineTestProject)' != 'true' AND '$(OutputType)' == 'Exe' AND '$(TargetFrameworkIdentifier)' == '.NETCoreApp'">
    <Compile Include="$(MSBuildThisFileDirectory)Common\Program.cs" />
  </ItemGroup>

  <Import Project="..\Directory.Build.targets" />

</Project>
