# Pipeline: https://dev.azure.com/dnceng/internal/_build?definitionId=1544

trigger:
  batch: true
  branches:
    include:
    - dnup
    - release/dnup
    - release/dotnetup

pr:
  branches:
    include:
    - dnup
    - release/dnup
    - release/dotnetup

parameters:
# When true, runs the pipeline in the same way as the PR pipeline.
- name: runTestBuild
  displayName: Run A Test Build
  type: boolean
  default: false
- name: enableArm64Job
  displayName: Enables the ARM64 job
  type: boolean
  default: false
- name: enableAkaMsProbe
  displayName: Enable aka.ms API probe (manual only)
  type: boolean
  default: false

variables:
- template: /eng/pipelines/templates/variables/sdk-defaults.yml
# Variables used: DncEngInternalBuildPool
- template: /eng/common/templates-official/variables/pool-providers.yml
# Helix testing requires a token when internally run.
# Variables used: HelixApiAccessToken
- group: DotNet-HelixApi-Access
- group: AzureDevOps-Artifact-Feeds-Pats
# Allows Arcade to run a signed build by disabling post-build signing for release-branch builds or manual builds that are not running tests.
- ${{ if and(eq(parameters.runTestBuild, false), or(startswith(variables['Build.SourceBranch'], 'refs/heads/release/'), startswith(variables['Build.SourceBranch'], 'refs/heads/internal/release/'), eq(variables['Build.Reason'], 'Manual'))) }}:
  - name: PostBuildSign
    value: false
# Provides TSA variables for automatic bug reporting.
- ${{ if ne(variables['Build.Reason'], 'PullRequest') }}:
  - group: DotNet-CLI-SDLValidation-Params
### LOCAL ONLY ###
- name: _publishArgument
  value: -publish
- name: _signArgument
  value: -sign
- name: _TeamName
  value: DotNet-Cli
# MicroBuildSigningPlugin@4 on non-Windows agents requires a pipeline variable
# literally named TeamName (separate from arcade's _TeamName convention).
- name: TeamName
  value: DotNet-Cli
- name: _BuildConfig
  value: Release
- name: _officialBuildProperties
  # The OfficialBuilder property is set to Microsoft for the official build only.
  # This property is checked in Directory.Build.props and adds the MICROSOFT_ENABLE_TELEMETRY constant.
  # This constant is used in CompileOptions.cs to set both TelemetryOptOutDefault and TelemetryOptOutDefaultString.
  value: /p:DotNetPublishUsingPipelines=true /p:OfficialBuilder=Microsoft /p:OfficialBuildId=$(Build.BuildNumber)
- name: _SignType
  value: real

resources:
  repositories:
  - repository: 1esPipelines
    type: git
    name: 1ESPipelineTemplates/1ESPipelineTemplates
    ref: refs/tags/release

extends:
  ${{ if ne(variables['Build.Reason'], 'PullRequest') }}:
    template: v1/1ES.Official.PipelineTemplate.yml@1esPipelines
  ${{ else }}:
    template: v1/1ES.Unofficial.PipelineTemplate.yml@1esPipelines
  parameters:
    containers:
      azureLinux30Amd64:
        image: mcr.microsoft.com/dotnet-buildtools/prereqs:azurelinux-3.0-net10.0-build-amd64
      # Cross-build containers for NAOT executables (link against older glibc/musl sysroots).
      # See: https://github.com/dotnet/runtime/blob/main/docs/workflow/using-docker.md
      crossBuildLinuxX64:
        image: mcr.microsoft.com/dotnet-buildtools/prereqs:azurelinux-3.0-net10.0-cross-amd64
        env:
          ROOTFS_DIR: /crossrootfs/x64
      crossBuildLinuxArm64:
        image: mcr.microsoft.com/dotnet-buildtools/prereqs:azurelinux-3.0-net10.0-cross-arm64
        env:
          ROOTFS_DIR: /crossrootfs/arm64
      crossBuildMuslX64:
        image: mcr.microsoft.com/dotnet-buildtools/prereqs:azurelinux-3.0-net10.0-cross-amd64-musl
        env:
          ROOTFS_DIR: /crossrootfs/x64
      crossBuildMuslArm64:
        image: mcr.microsoft.com/dotnet-buildtools/prereqs:azurelinux-3.0-net10.0-cross-arm64-musl
        env:
          ROOTFS_DIR: /crossrootfs/arm64

    sdl:
      sourceAnalysisPool:
        name: $(DncEngInternalBuildPool)
        image: 1es-windows-2022
        os: windows
      policheck:
        enabled: true
      tsa:
        enabled: true
      binskim:
        enabled: true
      ${{ if or(eq(parameters.runTestBuild, true), eq(variables['Build.Reason'], 'PullRequest')) }}:
        componentgovernance:
          # Refdoc: https://docs.opensource.microsoft.com/tools/cg/component-detection/variables/
          ignoreDirectories: artifacts, .packages

    stages:
      - stage: style_check
        displayName: 📏 Style check
        jobs:
        - template: /eng/common/templates-official/job/job.yml@self
          parameters:
            displayName: '📏 dotnetup style check'
            pool:
              name: $(DncEngInternalBuildPool)
              image: 1es-windows-2022
              os: windows
            helixRepo: dotnet/sdk
            timeoutInMinutes: 15
            enableMicrobuild: false
            enablePublishBuildAssets: false
            enableTelemetry: true
            enableSbom: false
            steps:
            - powershell: |
                & .\restore.cmd
              displayName: 🍱 Bootstrap toolset
            - powershell: |
                & .\.dotnet\dotnet restore test\dotnetup.Tests\dotnetup.Tests.csproj
              displayName: ♻️ Restore
            - powershell: |
                $failed = $false
                & .\.dotnet\dotnet build src\Installer\Microsoft.Dotnet.Installation\Microsoft.Dotnet.Installation.csproj -warnaserror --no-restore /p:EnforceCodeStyleInBuild=true /p:CodeAnalysisTreatWarningsAsErrors=true
                if ($LASTEXITCODE -ne 0) { $failed = $true }
                & .\.dotnet\dotnet build src\Installer\dotnetup.Library\dotnetup.Library.csproj -warnaserror --no-restore /p:EnforceCodeStyleInBuild=true /p:CodeAnalysisTreatWarningsAsErrors=true
                if ($LASTEXITCODE -ne 0) { $failed = $true }
                & .\.dotnet\dotnet build test\dotnetup.Tests\dotnetup.Tests.csproj -warnaserror --no-restore /p:EnforceCodeStyleInBuild=true /p:CodeAnalysisTreatWarningsAsErrors=true
                if ($LASTEXITCODE -ne 0) { $failed = $true }
                if ($failed) { Write-Error 'One or more projects have warnings. Fix all warnings to pass this check.'; exit 1 }
              displayName: 🔨 Build with -warnaserror

      - stage: tests
        displayName: 🧪 dotnetup tests
        dependsOn: []
        jobs:
        ### Windows ###
        - template: /eng/pipelines/templates/jobs/dotnetup/dotnetup-tests.yml@self
          parameters:
            pool:
              name: $(DncEngInternalBuildPool)
              image: windows.vs2026.amd64
              os: windows
              emoji: 🪟
            helixTargetQueue: windows.amd64.vs2026.pre.scout
            oneESCompat:
              templateFolderName: templates-official
              publishTaskPrefix: 1ES.
            runtimeSourceProperties: /p:DotNetRuntimeSourceFeed=https://ci.dot.net/internal /p:DotNetRuntimeSourceFeedKey=$(dotnetbuilds-internal-container-read-token-base64)
            populateInternalRuntimeVariables: true
            # WORKAROUND: BinSkim requires the folder exist prior to scanning.
            preSteps:
            - powershell: New-Item -ItemType Directory -Path $(Build.SourcesDirectory)/artifacts/bin -Force
              displayName: Create artifacts/bin directory
            ${{ if and(eq(parameters.runTestBuild, false), ne(variables['Build.Reason'], 'PullRequest')) }}:
              timeoutInMinutes: 180
              windowsJobParameterSets:
              ### OFFICIAL ###
              - categoryName: Official
                publishArgument: $(_publishArgument)
                signArgument: $(_signArgument)
                officialBuildProperties: $(_officialBuildProperties) /p:BuildWorkloads=true
                enableDefaultArtifacts: true
                runTests: false
                publishRetryConfig: true
                variables:
                  _SignType: real
      ############### BUILD / PACK / SIGN / PUBLISH ###############
      # Standard Arcade flow.  Set projectsArgument to only build dotnetup, not the whole SDK.
      - stage: build_dotnetup
        displayName: 🛠️ Build dotnetup
        dependsOn: []
        jobs:
        ############### WINDOWS ###############
        - template: /eng/pipelines/templates/jobs/sdk-job-matrix.yml@self
          parameters:
            pool:
              name: $(DncEngInternalBuildPool)
              image: windows.vs2026.amd64
              os: windows
            oneESCompat:
              templateFolderName: templates-official
              publishTaskPrefix: 1ES.
            runtimeSourceProperties: /p:DotNetRuntimeSourceFeed=https://ci.dot.net/internal /p:DotNetRuntimeSourceFeedKey=$(dotnetbuilds-internal-container-read-token-base64)
            populateInternalRuntimeVariables: true
            preSteps:
            - powershell: New-Item -ItemType Directory -Path $(Build.SourcesDirectory)/artifacts/bin -Force
              displayName: Create artifacts/bin directory
            timeoutInMinutes: 90
            windowsJobParameterSets:
            - categoryName: Dotnetup
              targetArchitecture: x64
              publishArgument: $(_publishArgument)
              signArgument: $(_signArgument)
              officialBuildProperties: $(_officialBuildProperties) /p:PublishBinariesAndBadge=false
              projectsArgument: /p:Projects=$(Build.SourcesDirectory)/dotnetup.slnf
              enableDefaultArtifacts: true
              runTests: false
              variables:
                _SignType: real
            - categoryName: Dotnetup
              targetArchitecture: arm64
              publishArgument: $(_publishArgument)
              signArgument: $(_signArgument)
              officialBuildProperties: $(_officialBuildProperties) /p:PublishBinariesAndBadge=false
              projectsArgument: /p:Projects=$(Build.SourcesDirectory)/dotnetup.slnf
              enableDefaultArtifacts: true
              runTests: false
              variables:
                _SignType: real

        ############### LINUX ###############
        - template: /eng/pipelines/templates/jobs/sdk-job-matrix.yml@self
          parameters:
            pool:
              name: $(DncEngInternalBuildPool)
              image: 1es-ubuntu-2204
              os: linux
            oneESCompat:
              templateFolderName: templates-official
              publishTaskPrefix: 1ES.
            runtimeSourceProperties: /p:DotNetRuntimeSourceFeed=https://ci.dot.net/internal /p:DotNetRuntimeSourceFeedKey=$(dotnetbuilds-internal-container-read-token-base64)
            populateInternalRuntimeVariables: true
            enableMicrobuildForMacAndLinux: true
            timeoutInMinutes: 90
            linuxJobParameterSets:
            # Cross-build containers set ROOTFS_DIR; pass /p:SysRoot and /p:LinkerFlavor=lld
            # via osProperties so the NativeAOT linker targets the correct sysroot for the RID.
            - categoryName: Dotnetup
              container: crossBuildLinuxX64
              targetArchitecture: x64
              runtimeIdentifier: linux-x64
              publishArgument: $(_publishArgument)
              signArgument: $(_signArgument)
              officialBuildProperties: $(_officialBuildProperties) /p:PublishBinariesAndBadge=false
              projectsArgument: /p:Projects=$(Build.SourcesDirectory)/dotnetup.slnf
              osProperties: /p:SysRoot=$ROOTFS_DIR /p:LinkerFlavor=lld
              enableDefaultArtifacts: true
              runTests: false
            - categoryName: Dotnetup
              container: crossBuildLinuxArm64
              targetArchitecture: arm64
              runtimeIdentifier: linux-arm64
              publishArgument: $(_publishArgument)
              signArgument: $(_signArgument)
              officialBuildProperties: $(_officialBuildProperties) /p:PublishBinariesAndBadge=false
              projectsArgument: /p:Projects=$(Build.SourcesDirectory)/dotnetup.slnf
              osProperties: /p:SysRoot=$ROOTFS_DIR /p:LinkerFlavor=lld
              enableDefaultArtifacts: true
              runTests: false
            - categoryName: Dotnetup
              container: crossBuildMuslX64
              targetArchitecture: x64
              runtimeIdentifier: linux-musl-x64
              publishArgument: $(_publishArgument)
              signArgument: $(_signArgument)
              officialBuildProperties: $(_officialBuildProperties) /p:PublishBinariesAndBadge=false
              projectsArgument: /p:Projects=$(Build.SourcesDirectory)/dotnetup.slnf
              osProperties: /p:SysRoot=$ROOTFS_DIR /p:LinkerFlavor=lld /p:OSName=linux-musl
              enableDefaultArtifacts: true
              enableSbom: false
              runTests: false
            - categoryName: Dotnetup
              container: crossBuildMuslArm64
              targetArchitecture: arm64
              runtimeIdentifier: linux-musl-arm64
              publishArgument: $(_publishArgument)
              signArgument: $(_signArgument)
              officialBuildProperties: $(_officialBuildProperties) /p:PublishBinariesAndBadge=false
              projectsArgument: /p:Projects=$(Build.SourcesDirectory)/dotnetup.slnf
              osProperties: /p:SysRoot=$ROOTFS_DIR /p:LinkerFlavor=lld /p:OSName=linux-musl
              enableDefaultArtifacts: true
              enableSbom: false
              runTests: false

        ############### MACOS ###############
        - template: /eng/pipelines/templates/jobs/sdk-job-matrix.yml@self
          parameters:
            pool:
              name: Azure Pipelines
              image: macOS-latest
              os: macOS
            oneESCompat:
              templateFolderName: templates-official
              publishTaskPrefix: 1ES.
            runtimeSourceProperties: /p:DotNetRuntimeSourceFeed=https://ci.dot.net/internal /p:DotNetRuntimeSourceFeedKey=$(dotnetbuilds-internal-container-read-token-base64)
            populateInternalRuntimeVariables: true
            enableMicrobuildForMacAndLinux: true
            timeoutInMinutes: 90
            macOSJobParameterSets:
            - categoryName: Dotnetup
              targetArchitecture: x64
              runtimeIdentifier: osx-x64
              publishArgument: $(_publishArgument)
              signArgument: $(_signArgument)
              officialBuildProperties: $(_officialBuildProperties) /p:PublishBinariesAndBadge=false
              projectsArgument: /p:Projects=$(Build.SourcesDirectory)/dotnetup.slnf
              enableDefaultArtifacts: true
              runTests: false
            - categoryName: Dotnetup
              targetArchitecture: arm64
              runtimeIdentifier: osx-arm64
              publishArgument: $(_publishArgument)
              signArgument: $(_signArgument)
              officialBuildProperties: $(_officialBuildProperties) /p:PublishBinariesAndBadge=false
              projectsArgument: /p:Projects=$(Build.SourcesDirectory)/dotnetup.slnf
              enableDefaultArtifacts: true
              runTests: false

      ############### PUBLISH STAGE ###############
      - stage: publish
        displayName: 📤 Publish to BAR
        dependsOn:
        - build_dotnetup
        jobs:
        - template: /eng/common/templates-official/job/publish-build-assets.yml@self
          parameters:
            publishUsingPipelines: true
            publishAssetsImmediately: true
            isAssetlessBuild: false
            repositoryAlias: self
            pool:
              name: $(DncEngInternalBuildPool)
              image: 1es-windows-2022
              os: windows

      - ${{ if eq(parameters.enableAkaMsProbe, true) }}:
        - stage: aka_ms_probe
          displayName: 🔗 aka.ms API probe
          dependsOn: []
          condition: and(succeeded(), eq(variables['Build.Reason'], 'Manual'), ne(variables['Build.Reason'], 'PullRequest'))
          jobs:
          - template: /eng/common/templates-official/job/job.yml@self
            parameters:
              name: aka_ms_probe
              displayName: Create, update, verify, and delete a build-unique aka.ms link
              pool:
                name: $(DncEngInternalBuildPool)
                image: 1es-windows-2022
                os: windows
              steps:
              - task: AzureCLI@2
                displayName: Probe aka.ms Redirection API
                inputs:
                  azureSubscription: "Darc: Maestro Production"
                  scriptType: ps
                  scriptLocation: inlineScript
                  inlineScript: |
                    $ErrorActionPreference = 'Stop'

                    $tenantId = '72f988bf-86f1-41af-91ab-2d7cd011db47'
                    $redirectionApiResource = 'https://msazurecloud.onmicrosoft.com/RedirectionMgmtApi-Prod'
                    $apiBaseUrl = "https://redirectionapi-ame.trafficmanager.net/api/aka/1/$tenantId"
                    $shortUrl = "dotnet/dotnetup-probe/$(Build.BuildId)-$(System.JobAttempt)/dotnetup-win-x64.exe"
                    $akaMsUrl = "https://aka.ms/$shortUrl"
                    $firstTargetUrl = 'https://ci.dot.net/public/dotnetup/0.1.4-preview.6.26316.3/dotnetup-win-x64.exe'
                    $secondTargetUrl = 'https://ci.dot.net/public/dotnetup/0.1.4-preview.4.26303.1/dotnetup-win-x64.exe'
                    $owners = 'dotnet@microsoft.com'
                    $createdBy = 'dotnet-dnceng'
                    $groupOwner = 'dotnet'

                    function New-AkaMsPayload([string] $targetUrl, [bool] $isUpdate) {
                      $link = @{
                        shortUrl = $shortUrl
                        owners = $owners
                        targetUrl = $targetUrl
                        lastModifiedBy = $createdBy
                        description = "Temporary dotnetup aka.ms probe for Azure DevOps build $(Build.BuildId)"
                        groupOwner = $groupOwner
                        isAllowParam = $true
                      }

                      if (-not $isUpdate) {
                        $link.createdBy = $createdBy
                        $link.isVanity = $true
                      }

                      return @($link) | ConvertTo-Json -Depth 4 -Compress
                    }

                    function Invoke-AkaMsRequest([string] $method, [string] $path, [string] $body) {
                      $uri = "$apiBaseUrl/$path"
                      $headers = @{ Authorization = "Bearer $accessToken" }
                      if ([string]::IsNullOrEmpty($body)) {
                        return Invoke-WebRequest -UseBasicParsing -Uri $uri -Headers $headers -Method $method
                      }

                      return Invoke-WebRequest -UseBasicParsing -Uri $uri -Headers $headers -Method $method -ContentType 'application/json' -Body $body
                    }

                    function Assert-AkaMsTarget([string] $expectedTargetUrl) {
                      $lastResolvedTargetUrl = ''
                      for ($attempt = 1; $attempt -le 12; $attempt++) {
                        $probeUrl = "$akaMsUrl?probe=$(Build.BuildId)-$(System.JobAttempt)-$attempt"
                        $resolvedTargetUrl = & curl.exe -s -L -I -o NUL -w '%{url_effective}' $probeUrl
                        if ($LASTEXITCODE -ne 0) {
                          throw "curl failed while resolving $probeUrl. Exit code: $LASTEXITCODE"
                        }

                        $lastResolvedTargetUrl = $resolvedTargetUrl
                        $resolvedTargetWithoutQuery = ([Uri] $resolvedTargetUrl).GetLeftPart([UriPartial]::Path)
                        if ($resolvedTargetWithoutQuery -eq $expectedTargetUrl) {
                          return
                        }

                        Write-Host "Attempt $attempt did not observe the expected redirect yet. Resolved target: $resolvedTargetUrl"
                        Start-Sleep -Seconds 10
                      }

                      throw "Expected $akaMsUrl to resolve to $expectedTargetUrl, but the last observed target was $lastResolvedTargetUrl."
                    }

                    Write-Host "Requesting an access token for the aka.ms Redirection API resource."
                    $accessToken = & az account get-access-token --resource $redirectionApiResource --query accessToken -o tsv
                    if ($LASTEXITCODE -ne 0 -or [string]::IsNullOrEmpty($accessToken)) {
                      throw 'Failed to acquire an access token for the aka.ms Redirection API resource.'
                    }

                    $created = $false
                    try {
                      Write-Host "Creating temporary aka.ms/$shortUrl -> $firstTargetUrl"
                      Invoke-AkaMsRequest 'Post' 'bulk' (New-AkaMsPayload $firstTargetUrl $false) | Out-Null
                      $created = $true
                      Assert-AkaMsTarget $firstTargetUrl

                      Write-Host "Updating temporary aka.ms/$shortUrl -> $secondTargetUrl"
                      Invoke-AkaMsRequest 'Put' 'bulk' (New-AkaMsPayload $secondTargetUrl $true) | Out-Null
                      Assert-AkaMsTarget $secondTargetUrl
                    }
                    finally {
                      if ($created) {
                        Write-Host "Deleting temporary aka.ms/$shortUrl"
                        Invoke-AkaMsRequest 'Delete' "harddelete/$shortUrl" $null | Out-Null
                      }
                    }
