variables:
  ############### BUILD ###############
  buildConfiguration: Release
  # buildArchitecture: x64
  # runTests: true
  # testFullMSBuild: false
  # runAoTTests: false

  # TODO: Instead, just have this always on the build and set the value, same with RunTestsAsTool.
  # testInBuildProperties: /p:Test=false
  # testName: HelixTests
  # testProjects: $(Build.SourcesDirectory)/test/UnitTests.proj

  # runtimeSourceProperties: ''
  # runtimeIdentifierProperties: ''

  # publishArgument: ''
  # officialBuildProperties: ''

  ############### ARCADE ###############
  # "Post-build signing" means that this build will NOT sign itself.
  PostBuildSign: true
  # Both this (used in Arcade for the MicroBuildSigningPlugin) and DotNetSignType (used in Arcade in Sign.proj) are necessary to set the sign type.
  _SignType: test
  _TeamName: DotNetCore
  # This ends up being set by the DotNet-HelixApi-Access variable group when running internally.
  HelixApiAccessToken: ''

  ############### OTHER ###############
  Codeql.ExcludePathPatterns: artifacts, .packages