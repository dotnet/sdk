<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>$(AspNetTestTfm)</TargetFramework>
  </PropertyGroup>

  <PropertyGroup>
    <!-- We don't want to run build server when not running as tests. -->
    <UseRazorBuildServer>false</UseRazorBuildServer>
  </PropertyGroup>

  <Target Name="EnsurePackagesExist" BeforeTargets="Restore">
    <ItemGroup>
      <LocalPackages Include="$(AspNetTestPackageSource)\PackageLibraryDirectDependency.1.0.0.nupkg" />
      <LocalPackages Include="$(AspNetTestPackageSource)\PackageLibraryTransitiveDependency.1.0.0.nupkg" />
    </ItemGroup>

    <Error Condition="!Exists('%(LocalPackages.Identity)')" 
      Text="Package %(LocalPackages.Identity) does not exist. Ensure the test calls CreatePackCommand(ProjectDirectory, &quot;PackageLibraryDirectDependency&quot;)" />
  </Target>

  <ItemGroup>
    <PackageReference Include="PackageLibraryDirectDependency" Version="1.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\AnotherClassLib\AnotherClassLib.csproj" />
  </ItemGroup>

</Project>
