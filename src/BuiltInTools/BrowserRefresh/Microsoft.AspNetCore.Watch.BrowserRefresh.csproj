<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Intentionally pinned. This feature is supported in projects targeting 6.0 or newer.-->
    <TargetFramework>net6.0</TargetFramework>
    <StrongNameKeyId>MicrosoftAspNetCore</StrongNameKeyId>

    <IsPackable>false</IsPackable>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
    <EmbeddedResource Include="WebSocketScriptInjection.js" />
    <EmbeddedResource Include="BlazorHotReload.js" />
  </ItemGroup>

  <!-- Reference 6.0.2 Asp.Net targeting pack in Source Build - this was the earliest version after the API surface stabilized -->
  <ItemGroup Condition="'$(DotNetBuildFromSource)' == 'true'">
    <KnownFrameworkReference
      Update="Microsoft.AspNetCore.App"
      TargetingPackVersion="6.0.2" />
  </ItemGroup>

</Project>
