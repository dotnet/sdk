// <auto-generated>
// Generated by the .NET SDK for dotnet-watch. Do not edit.
//
// Pins the public half of the dotnet-watch browser tools key pair and the fixed provider route
// into the application build output, so the browser never learns them from the provider it
// authenticates. The key pair is generated by the build into the project's intermediate output;
// only this public half is ever served, and the private half is read exclusively by dotnet-watch.
//
// This module is produced by development builds and never reaches publish output. It is imported
// only after the browser tools settings document reports that a dotnet-watch session enabled the
// tools, so importing it is what actually starts them.
// </auto-generated>

import { startBrowserTools } from './__CLIENT_MODULE__';

export const browserToolsConfiguration = {
  publicKey: '__PUBLIC_KEY__',
  connectPath: '__ROUTE_PREFIX__/connect',
  clearCachePath: '__ROUTE_PREFIX__/clear-cache',
  moduleUrl: import.meta.url,
};

startBrowserTools(browserToolsConfiguration)
  .catch(error => console.debug('Unable to start the dotnet-watch browser tools.', error));
